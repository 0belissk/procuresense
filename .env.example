# Web clients should call the frontend dev server, not the backend directly.
API_BASE_URL=http://localhost:8080

# Backend-only secrets
OPENAI_API_KEY=
OPENAI_ENABLED=false
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/procuresense
SPRING_DATASOURCE_USERNAME=procure_local
SPRING_DATASOURCE_PASSWORD=procure_local

# Identity headers used for smoke tests and demo data commands
IDENTITY_ORG_ID=demo-org
IDENTITY_ROLE=admin
